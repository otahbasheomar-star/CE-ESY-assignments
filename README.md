# CE-ESY-assignments
This program implements a Circular Buffer in C.
The circular buffer is a fixed-size data structure that works in a FIFO (First In First Out) way, and it reuses empty spaces when data is removed.
